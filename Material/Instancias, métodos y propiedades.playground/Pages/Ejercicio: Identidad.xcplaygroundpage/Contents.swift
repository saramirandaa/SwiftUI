/*:
## Ejercicio: Identidad

 ¿Qué significa que un valor de instancia tenga su propia identidad?

 - callout(Ejercicio): 
 Crea una variable para `myPlans` (mis planes) que se inicialice en una cadena que describa los planes que tienes para la noche.\
 Crea una segunda variable para `friendPlans` (planes de amigo), pero que se inicialice en `myPlans` (mis planes). (Tu amigo tiene menos iniciativa que tú para planificar).\
 Debajo de la declaración de `friendPlans` (planes de amigo), actualiza `myPlans` (mis planes) utilizando el operador de suma `+` para agregar una actividad más.\
 Comprueba los valores de `myPlans` (mis planes) y `friendPlans` (planes de amigo). ¿Son iguales o diferentes?
 */
// Crea tus variables aquí:

// Actualiza `myPlans` (mis planes) aquí:

/*:
 - callout(Ejercicio): 
 Crea una función `addDance` (agregar baile) que tome una cadena, que agregue una frase sobre baile (como “¡y a bailar!” o “pero sin bailar”, según lo que te guste) y que devuelva la cadena nueva.\
 Llama a la función `addDance` (agregar baile) en `myPlans` (mis planes) y asigna el resultado a `friendPlans` (planes de amigo).
 */
// Define y llama a tu función aquí:

/*:
 - callout(Ejercicio): 
 ¿Cómo esperas que cambie `friendPlans` (planes de amigo)? ¿Cómo esperas que cambie `myPlans` (mis planes)?\
 Imprime ambas instancias para averiguarlo.
 */
// Imprime aquí y comprueba si se cumplió lo que esperabas:

/*:
  _Copyright © 2020 Apple Inc._

 _El presente documento otorga permiso, libre de cargos, a cualquier persona que obtenga una copia de este software y de los archivos de documentación asociados (el "Software") para utilizar el Software sin restricciones, incluidos, sin limitación, derechos para usar, copiar, modificar, combinar, publicar, distribuir, sublicenciar o vender copias del Software, y para permitir hacerlo a las personas a las que se proporcione el Software, siempre que se cumplan las siguientes condiciones:_

 _El anterior aviso de copyright y este aviso de permiso deben incluirse en todas las copias o partes importantes del Software._

 _EL SOFTWARE ES PROPORCIONADO “TAL COMO ESTÁ”, SIN GARANTÍA DE NINGÚN TIPO, YA SEA EXPRESA O IMPLÍCITA, INCLUIDAS, ENTRE OTRAS, LAS GARANTÍAS DE COMERCIABILIDAD, DE IDONEIDAD PARA UN FIN ESPECÍFICO O DE NO VIOLACIONES DE LOS DERECHOS DE AUTOR. EN NINGÚN CASO LOS AUTORES O LOS TITULARES DEL COPYRIGHT SERÁN RESPONSABLES DE RECLAMOS, DAÑOS Y PERJUICIOS, U OTRAS RESPONSABILIDADES, YA SEA POR UNA ACCIÓN CONTRACTUAL, POR NEGLIGENCIA O POR OTROS MOTIVOS, QUE PUEDAN SURGIR EN RELACIÓN CON EL SOFTWARE, SU UTILIZACIÓN U OTRAS OPERACIONES LLEVADAS A CABO CON ÉL._
 */

/*:
[Anterior](@previous)  |  página 17 de 17
 */
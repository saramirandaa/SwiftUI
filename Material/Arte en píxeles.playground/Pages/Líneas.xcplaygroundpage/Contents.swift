/*:
## Líneas

 Es divertido realizar operaciones en un píxel por vez, pero tendrás que ser más eficiente si quieres crear gráficos más complejos.
 
 Comienza creando funciones para hacer líneas horizontales y verticales. El código de la función de línea horizontal ya está hecho.

 - callout(Ejercicio): Completa la función `hLine(x:y:length:color)` (línea horizontal[x:y:largo:color]). Luego, haz una función `vLine(x:y:length:color:)` (línea vertical[x:y:largo:color:]) para dibujar líneas verticales.
 */
func hline(x: Int, y: Int, length: Int, color: Color) {
    for i in 0 ... length - 1 {
        // Establece el siguiente píxel a lo largo de la línea horizontal
    }
}

//:  - callout(Ejercicio): Prueba usar las funciones de línea para crear un nuevo gráfico.

//: A continuación, crea bloques rectangulares.

/*:
[Anterior](@previous)  |  página 4 de 13  |  [Siguiente: Bloques de color](@next)
 */
/*:
## Contar programas

 En esta página, crearás un algoritmo para tabular las respuestas de una encuesta simulada y mostrar los resultados. Usarás los mismos procedimientos básicos en las páginas siguientes.

 Hay una constante del arreglo `randomShowData` (datos aleatorios del programa), que simula los resultados de una encuesta con 10 respuestas de modo que puedas probar tu código. Los resultados serán distintos cada vez que ejecutes el código. (Si quieres, puedes imprimir la constante del arreglo `randomShowData` en la consola para realizar una verificación).
 */
randomShowData
//:  - callout(Ejercicio): Usando los comentarios como guía, haz un recuento de los resultados de la encuesta simulada en `randomShowData`.
// Crea una instancia de Tabulator (tabulador).

// Ejecuta un ciclo a través de los programas de randomShowData, de modo que se incremente el recuento total de cada uno.

// Ejecuta un ciclo a través de los programas contados (almacenados en tabulator.values [valores del tabulador]), de modo que se imprima la información de cada uno.

/*:
 - callout(Nota): También hay una constante `randomShowDataLargeSet` (conjunto grande de datos aleatorios del programa) que simula 1,000 resultados de la encuesta. Una vez que veas que el código se ejecuta correctamente y estés conforme, puedes actualizar tu código para tabular valores de ese arreglo en su lugar.

[Anterior](@previous)  |  página 3 de 11  |  [Siguiente: Errores de datos](@next)
 */